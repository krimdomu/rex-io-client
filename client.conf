{
   # the url pointing to Rex::IO::Server
   server => "http://localhost:5000",

   # path to service cache directory
   #cache_dir => "/var/lib/rex/io/cache/services",
   cache_dir => "/tmp/service-cache",
}
